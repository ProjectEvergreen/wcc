diff --git a/node_modules/@greenwood/plugin-import-jsx/src/index.js b/node_modules/@greenwood/plugin-import-jsx/src/index.js
index 5fe6e11..5e684c3 100644
--- a/node_modules/@greenwood/plugin-import-jsx/src/index.js
+++ b/node_modules/@greenwood/plugin-import-jsx/src/index.js
@@ -14,11 +14,11 @@ class ImportJsxResource {
     this.servePage = options.servePages ? "dynamic" : null;
   }
 
-  async shouldServe(url) {
+  async shouldServe(url, request) {
     const { pathname, protocol } = url;
     const ext = pathname.split(".").pop();
 
-    return protocol === "file:" && this.extensions.includes(ext);
+    return protocol === "file:" && this.extensions.includes(ext) && request.headers?.get("Accept").indexOf(this.contentType) >= 0;
   }
 
   async serve(url) {
